// Dashboard view layout
//
// The lobby/selection view for choosing kernels, contexts, and seats.
//
// Structure:
// ┌────────────────────────────────────────────────────────┐
// │                    Dashboard                           │
// │  ┌──────────┬───────────┬────────────────────────┐    │
// │  │ KERNELS  │ CONTEXTS  │      YOUR SEATS        │    │
// │  │          │           │                        │    │
// │  │ [lobby]  │ [default] │  • amy@local           │    │
// │  │ [test]   │ [coding]  │  • claude@prod         │    │
// │  │          │           │                        │    │
// │  └──────────┴───────────┴────────────────────────┘    │
// │  ┌────────────────────────────────────────────────┐   │
// │  │ Take a seat: [Take Seat 席]                    │   │
// │  └────────────────────────────────────────────────┘   │
// └────────────────────────────────────────────────────────┘
//
// Panel builders:
// - KernelList, ContextList, SeatsList: Column markers filled by filler systems
// - DashboardFooter: Footer with Take Seat button
//
// SeatSelector is in header chrome, not part of this layout.
(
    name: "dashboard",
    root: Container(
        direction: Column,
        children: [
            // Main content - three-column row with padding and gaps
            Container(
                direction: Row,
                children: [
                    Panel(id: "KernelList", flex: 1.0),
                    Panel(id: "ContextList", flex: 1.0),
                    Panel(id: "SeatsList", flex: 1.0),
                ],
                flex: 1.0,
                padding: 20.0,
                gap: 20.0,
            ),
            // Footer with Take Seat button
            Panel(id: "DashboardFooter", flex: 0.0),
        ],
        flex: 1.0,
    ),
    huds: [],
)
