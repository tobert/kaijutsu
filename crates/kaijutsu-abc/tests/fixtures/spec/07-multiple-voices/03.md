# Spec context: §7.0 — Multiple voices

## Source

- Upstream: <https://abcnotation.com/wiki/abc:standard:v2.1#multiple_voices>
- Cached at: `crates/kaijutsu-abc/docs/abc-spec-cache.md` (line 1912)
- Section: 7. Multiple voices

Reproduced verbatim from the ABC v2.1 standard by Chris Walshaw,
under CC BY-NC-SA 3.0 — see `tests/fixtures/spec/README.md`.

## Preceding prose

> Here follows the visible output:
> [![](/wiki/_media/abc:standard:multivoice-80.png)](/wiki/_detail/abc:standard:multivoice-80.png?id=abc%3Astandard%3Av2.1 "abc:standard:multivoice-80.png")
> `V:` can appear both in the body and the header. In the latter case, `V:` is used exclusively to set voice properties. For example, the `name` property in the example above, specifies which label should be printed on the first staff of the voice in question. Note that these properties may be also se
> Please note that the exact grouping of voices on the staff or staves is not specified by `V:` itself. This may be specified with the `%%score` [stylesheet directive](#stylesheet_directive_definition "abc:standard:v2.1 ↵"). See [voice grouping](#voice_grouping "abc:standard:v2.1 ↵") for details.
> For playback, see [instrumentation directives](#instrumentation_directives "abc:standard:v2.1 ↵") for details of how to assign a General MIDI instrument to a voice using a `%%MIDI` [stylesheet directive](#stylesheet_directive_definition "abc:standard:v2.1 ↵").
> Although it is not recommended, the [tune body](#tune_body_definition "abc:standard:v2.1 ↵") of fragment `X:1`, could also be notated this way:
