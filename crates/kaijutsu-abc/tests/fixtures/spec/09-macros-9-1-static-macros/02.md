# Spec context: §9.1 — Static macros

## Source

- Upstream: <https://abcnotation.com/wiki/abc:standard:v2.1#static_macros>
- Cached at: `crates/kaijutsu-abc/docs/abc-spec-cache.md` (line 2130)
- Section: 9. Macros
- Subsection: 9.1 Static macros

Reproduced verbatim from the ABC v2.1 standard by Chris Walshaw,
under CC BY-NC-SA 3.0 — see `tests/fixtures/spec/README.md`.

## Preceding prose

> You define a static macro by writing into the [tune header](#tune_header_definition "abc:standard:v2.1 ↵") something like this:
> When you play the tune, the program searches the [tune header](#tune_header_definition "abc:standard:v2.1 ↵") for macro definitions, then does a search and replace on its internal copy of the text before passing that to the parser which plays the tune. Every occurence of `~G3` in the tune is replace
> You can put in as many macros as you want, and indeed, if you only use static macros you will need to write a separate macro for each combination of pitch and note-length. Here is an example:
