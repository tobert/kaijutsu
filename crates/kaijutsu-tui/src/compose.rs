//! Compose — the modalkit `VimMachine` over the kernel-owned input block.
//!
//! Not built. The skeleton's placeholder is `app::Compose`, a single local
//! line submitted through `bridge::KernelBridge::send_prompt`; this module is
//! where the real surface lands. Spec: `docs/tui.md`, "Compose".
