//! The shell surface (`Ctrl+Z`) — kaish through `shell_execute`, the gated
//! path a human's shell already takes, with the acting-context and cwd
//! cursors rendered in its prompt.
//!
//! Not built. `Ctrl+Z` currently posts a status-line notice.
//! Spec: `docs/tui.md`, "Shell (`Ctrl+Z`)".
