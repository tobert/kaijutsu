[package]
name = "approval-ledger"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Durable SQLite-backed approval ledger for human-in-the-loop authorization of agent actions. DB-injected, no kaijutsu-* dependency — publishable standalone."

[lints]
workspace = true

[dependencies]
rusqlite = { workspace = true }
serde = { workspace = true }
strum = { workspace = true }
thiserror = { workspace = true }
sha2 = "0.10"
uuid = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
